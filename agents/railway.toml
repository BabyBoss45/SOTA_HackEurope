[build]
builder = "dockerfile"
dockerfilePath = "agents/Dockerfile"
watchPatterns = ["agents/**"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
numReplicas = 1
